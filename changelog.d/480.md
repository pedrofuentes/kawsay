- Internal hardening: model-download failures now cross to the app only as a typed category, and diagnostic logs record a request's origin (never a full URL) or a redacted error shape — so a raw path or message can never reach a log or the renderer.
