- If saving a favourite ever gets stuck (for example while a large import is busy), the heart now recovers on its own after a short wait instead of staying greyed out — it gently lets you know nothing was lost, so you can try again.
