- Internal (CI): the library-backed end-to-end user journeys now actually run in CI — the e2e job rebuilds the native catalog addon for Electron's ABI (instead of auto-skipping) and exercises the journeys headless on macOS, Windows and Linux, uploading Playwright traces + screenshots when a run fails.
